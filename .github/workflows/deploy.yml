name: Deploy to EC2

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "StrictHostKeyChecking=no" >> ~/.ssh/config

      - name: Deploy build to EC2
        run: |
          # Create temp folder and clean it
          ssh ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            mkdir -p /var/www/my-react-app-temp
            rm -rf /var/www/my-react-app-temp/*
          EOF

          # Copy build folder from repo to EC2 temp folder
          scp -r build/* ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/var/www/my-react-app-temp/

          # Move build files to production folder and restart Nginx
          ssh ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            sudo rm -rf /var/www/my-react-app/*
            sudo cp -r /var/www/my-react-app-temp/* /var/www/my-react-app/
            sudo systemctl restart nginx
          EOF
